import React from 'react';
import PlaceholderScreen from '../../shared/components/PlaceholderScreen';

export default function CreateNoticeScreen({ navigation, route }) {
  return (
    <PlaceholderScreen
      title="Create Notice"
      navigation={navigation}
      icon="add-circle-outline"
    />
  );
}
