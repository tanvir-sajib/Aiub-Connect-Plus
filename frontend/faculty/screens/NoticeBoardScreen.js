import React from 'react';
import PlaceholderScreen from '../../shared/components/PlaceholderScreen';

export default function NoticeBoardScreen({ navigation, route }) {
  return (
    <PlaceholderScreen
      title="Notice Board"
      navigation={navigation}
      icon="clipboard-outline"
    />
  );
}
